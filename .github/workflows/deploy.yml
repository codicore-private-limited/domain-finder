name: Deploy to Azure (disabled - use azure-deploy.yml)

on:
  workflow_dispatch:
    inputs:
      disabled:
        description: 'This workflow is disabled, use azure-deploy.yml'
        required: false

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Use azure-deploy.yml workflow instead'
